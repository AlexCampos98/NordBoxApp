<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nordboxapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--    Permisos camara-->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NordBoxApp"
        android:usesCleartextTraffic="true">

        <activity android:name=".usuarioActivity" />
        <activity android:name=".benchmarkEjercicioActivity" />
        <activity android:name=".menuActivity" />
        <activity android:name=".benchmarksActivity" />
        <activity android:name=".creacionUsuario" />
        <activity android:name=".login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

<!--        Llamada a la libreria para el login, imagen-->
        <activity android:name="com.theartofdev.edmodo.cropper.CropImageActivity"/>
<!--            android:theme="@style/AppTheme"/>-->
    </application>

</manifest>